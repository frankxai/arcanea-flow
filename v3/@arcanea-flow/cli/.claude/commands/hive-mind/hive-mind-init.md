# hive-mind-init

Initialize the Hive Mind collective intelligence system.

## Usage
```bash
npx arcanea-flow hive-mind init [options]
```

## Options
- `--force` - Force reinitialize
- `--config <file>` - Configuration file

## Examples
```bash
npx arcanea-flow hive-mind init
npx arcanea-flow hive-mind init --force
```
